import SwiftUI

@main
struct OutlineGroupApp: App {
    var body: some Scene {
        Window("OutlineGroup", id: "main") {
            ContentView()
        }
        .defaultSize(width: 300, height: 400)
    }
}